---
import BaseLayout from "../../layouts/BaseLayout.astro";
import "../../styles/projects/jijitsu.css";
import Rabbit from "./rabbit.astro";



const criminalPoster = [
  "/assets/what/jijitsu/contents/ciriminal-posters/boxing.jpg",
  "/assets/what/jijitsu/contents/ciriminal-posters/gallery.jpg",
  "/assets/what/jijitsu/contents/ciriminal-posters/hiring.jpg",
  "/assets/what/jijitsu/contents/ciriminal-posters/jazz-concert.jpg",
  "/assets/what/jijitsu/contents/ciriminal-posters/margot.jpg",
  "/assets/what/jijitsu/contents/ciriminal-posters/math-tutor.jpg",
  "/assets/what/jijitsu/contents/ciriminal-posters/party.jpg",
  "/assets/what/jijitsu/contents/ciriminal-posters/shoplifters.jpg",
  "/assets/what/jijitsu/contents/ciriminal-posters/missing.jpg",
  

];

const dreamMapping = [
  "/assets/what/jijitsu/contents/dream-mapping/letters.jpg",
  "/assets/what/jijitsu/contents/dream-mapping/map.jpg",
  "/assets/what/jijitsu/contents/dream-mapping/orange-dog.JPG",
  "/assets/what/jijitsu/contents/dream-mapping/soil.jpg",
];

const gifSrc = "/assets/what/jijitsu/contents/room/GIF/visitor1.gif";
const title = "JIJITSU";
---

<BaseLayout title={`${title} — Nuus`} description="Project page" current="what">
  <main class="project project--wide" id="top">
    <!-- Back to Top Button -->
    <a href="#top" class="back-to-top" id="back-to-top" aria-label="Back to top">
      <span class="back-to-top-text">TOP</span>
      <img src="/assets/what/jijitsu/contents/arrow.svg" alt="Back to top" class="back-to-top-arrow" />
    </a>
    
    <!-- Main grid wrapper containing title column + all content -->
    <div class="page-grid">
      <!-- LEFT: Sticky vertical title that spans all sections -->
      <div class="hero-title">
        <h1 class="title-vertical" id="page-title">{title}</h1>
      </div>

      <!-- RIGHT: All content sections -->
      <div class="page-content">
        <!-- Hero Section -->
        <section class="hero-section">
          <div class="gif-wrap">
            <img
              class="gif"
              src={gifSrc}
              alt="Jijitsu animation"
              loading="eager"
              decoding="async"
            />
          </div>

          <div class="desc">
          <p class="desc-lead">
           このこじんまりとした空間には、キッチン、トイレ、ロフトが備わり、一見すると誰でも住めそうなアパートの一室のように見える。
しかし、部屋の半分には白い布で覆われた土台のようなものが佇み、まず座る場所が見当たらない。

​
          </p>
          <p class="desc-lead">
床には土ごと置かれた植物、牛乳らしきものが入った瓶、彫刻のように形作られた粘土が点在し、生活感がありながらも、どこか異様な気配が漂う。
          </p>
       
          <p class="desc-lead">
来場者には、「かつてUSAGIたちという存在が住処としていたとされる空間の痕跡」が再構築されたものであると説明される。   </p>

      
            <p class="desc-lead">
そして、こう問いかけられる。

​

「もし、あなたがここに住むとしたら？ ここを自分なりに快適に過ごすとしたら、何をするか？」

USAGIたちが残した"らしき"ものの用途や意味は明かされず、それらはただ、そこにある。

 </p>
            <p class="desc-lead">
来場者は、その空間にあるものを観察し、触れ、試しながら、自分なりの解釈や物語を生み出していく。

​

ここにあるものは、かつて存在したものなのか、それとも今ここで生まれたものなのか。
その答えを決めるのは、ここにいる一人ひとりの視点と体験である。

​

フィクションとは、再構築された現実であり、そこに触れることで生まれる新たな認識こそが、自分自身の「自実（Self-reality）」となる。

​</p>
          </div>

          <div class="desc-meta">
            <p><span>Year</span> 2026</p>
            <p><span>Type</span> Installation / Prototype</p>
            <p><span>Role</span> Concept, Dev, Design</p>
          </div>
        </section>

        <!-- PROJECT SECTIONS -->
        <div class="projects-container">
          <!-- WANTED CRIMINALS PROJECT -->
          <article class="project-section" data-project="criminals">
            <button class="project-header" aria-expanded="false">
              <h2 class="project-title">Wanted Criminals</h2>
              <span class="project-toggle">+</span>
            </button>
            
            <div class="project-content">
              <div class="project-intro">
                <div class="intro-thumbnail">
                  <img
                    src="/assets/what/jijitsu/thumbnails/IMG_3734.jpg"
                    alt="Wanted Criminals thumbnail"
                    loading="lazy"
                    decoding="async"
                  />
                </div>
                <div class="intro-text">
                  <p class="section-desc">
                   「Wanted Criminals」 は、指名手配写真のもつフィクション性を再解釈し、デジタル処理を施して新たな文脈に置き換えたポスターシリーズである。


本作では、犯罪者の「像」として機能する指名手配写真をデータとして分解し、ランダムな改変を加えることで、視覚的な明確さを揺るがし、写真が持つ意味や役割を変容させている。

​

こうして再構築されたポスターは、求人広告やプロモーションビジュアルといった異なるフォーマットへと組み替えられ、元の写真が持っていた機能や権威を逸脱していく。


情報がどのように操作され、異なる文脈の中で受容されるのか——本作は、視覚的な信頼性とその流動性を探る試みである。
                  </p>
                </div>
              </div>

              <div class="project-gallery criminal-gallery">
                {criminalPoster.map((src, index) => {
                  const webpSrc = src.replace('.jpg', '.webp');
                  return (
                    <figure class="gallery-item">
                      <picture>
                        <source srcset={webpSrc} type="image/webp" />
                        <img
                          src={src}
                          alt="Jijitsu documentation"
                          loading="lazy"
                          decoding="async"
                        />
                      </picture>
                    </figure>
                  );
                })}
              </div>
            </div>
          </article>

          <!-- DREAM MAPPING PROJECT -->
          <article class="project-section" data-project="dream">
            <button class="project-header" aria-expanded="false">
              <h2 class="project-title">Dream Mapping</h2>
              <span class="project-toggle">+</span>
            </button>
            
            <div class="project-content">
              <div class="project-intro">
                <div class="intro-text full-width">
                  <p class="section-desc">
                    USAGIたちが記録していたとされる「夢の地図」。それは言葉と図形が交錯し、論理的な説明を拒む断片的な記録である。
                    
                    ここに描かれているのは、場所なのか、記憶なのか、それとも誰かの想像の産物なのか。見る者は、その解釈を委ねられ、自らの物語を紡ぐことになる。
                    
                    夢とは、語られることで形を成し、共有されることで新たな意味を持つ——「Dream Mapping」は、その流動性を可視化する試みである。
                  </p>
                </div>
              </div>

              <div class="project-gallery dream-gallery">
                <div class="dream-slides">
                  {dreamMapping.map((src, index) => (
                    <div class="dream-slide" data-index={index}>
                      <img
                        src={src}
                        alt={`Dream mapping ${index + 1}`}
                        loading="lazy"
                        decoding="async"
                      />
                    </div>
                  ))}
                </div>
                <div class="dream-controls">
                  <button class="dream-nav dream-prev" aria-label="Previous slide">←</button>
                  <div class="dream-indicators">
                    {dreamMapping.map((_, index) => (
                      <button class="dream-indicator" data-index={index} aria-label={`Go to slide ${index + 1}`}></button>
                    ))}
                  </div>
                  <button class="dream-nav dream-next" aria-label="Next slide">→</button>
                </div>
              </div>
            </div>
          </article>
        </div>

        <!-- RABBIT, HABIT, KUBID SECTION -->
        <Rabbit />
      </div>
    </div>
  </main>
</BaseLayout>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    // Project Section Toggle
    const projectSections = document.querySelectorAll('.project-section');
    
    projectSections.forEach(section => {
      const header = section.querySelector('.project-header');
      const content = section.querySelector('.project-content');
      const toggle = section.querySelector('.project-toggle');
      
      header?.addEventListener('click', () => {
        const isExpanded = header.getAttribute('aria-expanded') === 'true';
        
        // Toggle current section
        header.setAttribute('aria-expanded', String(!isExpanded));
        section.classList.toggle('expanded', !isExpanded);
        
        if (toggle) {
          toggle.textContent = isExpanded ? '+' : '−';
        }
        
        // Smooth scroll to section when opening
        if (!isExpanded) {
          setTimeout(() => {
            section.scrollIntoView({ behavior: 'smooth', block: 'start' });
          }, 100);
        }
      });
    });

    // Dream Mapping Auto-Slide Gallery
    const dreamSlides = document.querySelectorAll('.dream-slide');
    const dreamIndicators = document.querySelectorAll('.dream-indicator');
    const dreamPrev = document.querySelector('.dream-prev');
    const dreamNext = document.querySelector('.dream-next');
    
    if (dreamSlides.length > 0) {
      let currentSlide = 0;
      let autoSlideInterval: ReturnType<typeof setInterval>;
      
      const showSlide = (index: number) => {
        dreamSlides.forEach((slide, i) => {
          slide.classList.toggle('active', i === index);
        });
        dreamIndicators.forEach((indicator, i) => {
          indicator.classList.toggle('active', i === index);
        });
      };
      
      const nextSlide = () => {
        currentSlide = (currentSlide + 1) % dreamSlides.length;
        showSlide(currentSlide);
      };
      
      const prevSlide = () => {
        currentSlide = (currentSlide - 1 + dreamSlides.length) % dreamSlides.length;
        showSlide(currentSlide);
      };
      
      const goToSlide = (index: number) => {
        currentSlide = index;
        showSlide(currentSlide);
      };
      
      const startAutoSlide = () => {
        autoSlideInterval = setInterval(nextSlide, 4000);
      };
      
      const stopAutoSlide = () => {
        clearInterval(autoSlideInterval);
      };
      
      // Initialize
      showSlide(currentSlide);
      startAutoSlide();
      
      // Event listeners
      dreamNext?.addEventListener('click', () => {
        stopAutoSlide();
        nextSlide();
        startAutoSlide();
      });
      
      dreamPrev?.addEventListener('click', () => {
        stopAutoSlide();
        prevSlide();
        startAutoSlide();
      });
      
      dreamIndicators.forEach((indicator, index) => {
        indicator.addEventListener('click', () => {
          stopAutoSlide();
          goToSlide(index);
          startAutoSlide();
        });
      });
      
      // Pause on hover
      const dreamGallery = document.querySelector('.dream-gallery');
      dreamGallery?.addEventListener('mouseenter', stopAutoSlide);
      dreamGallery?.addEventListener('mouseleave', startAutoSlide);
    }

    // Dynamic Title Change and Dark Theme based on Rabbit Film Section
    const pageTitle = document.getElementById('page-title');
    const backToTop = document.getElementById('back-to-top');
    const rabbitFilmSection = document.querySelector('.rabbit-film');
    const projectMain = document.querySelector('.project');
    
    if (pageTitle && backToTop && rabbitFilmSection && projectMain) {
      let isInRabbitFilm = false;
      
      const changeTitle = (newTitle: string) => {
        // Fade out
        pageTitle.classList.add('fade-out');
        
        // Change text after fade out
        setTimeout(() => {
          pageTitle.textContent = newTitle;
          // Fade in
          pageTitle.classList.remove('fade-out');
        }, 200);
      };
      
      const observer = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting && !isInRabbitFilm) {
              isInRabbitFilm = true;
              changeTitle('Rabbit, habit, kubid');
              projectMain.classList.add('dark-theme');
              backToTop.classList.add('visible');
            } else if (!entry.isIntersecting && isInRabbitFilm) {
              isInRabbitFilm = false;
              changeTitle('JIJITSU');
              projectMain.classList.remove('dark-theme');
              backToTop.classList.remove('visible');
            }
          });
        },
        {
          threshold: 0.3,
          rootMargin: '-100px 0px -100px 0px'
        }
      );
      
      observer.observe(rabbitFilmSection);
    }

    // Hide header on scroll (20% viewport)
    const siteHeader = document.querySelector('.site-header');
    
    if (siteHeader) {
      const handleHeaderScroll = () => {
        const scrollThreshold = window.innerHeight * 0.2;
        
        if (window.scrollY > scrollThreshold) {
          siteHeader.classList.add('header-hidden');
        } else {
          siteHeader.classList.remove('header-hidden');
        }
      };
      
      window.addEventListener('scroll', handleHeaderScroll, { passive: true });
      handleHeaderScroll(); // Initial check
    }
  });
</script>